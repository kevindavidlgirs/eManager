<Window x:Class="prbd_1920_g04.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:prbd_1920_g04.Views"
        mc:Ignorable="d"
        Title="E-Club Manager" 
        Height="564.407" Width="861.017"
         WindowStartupLocation="CenterScreen">
    <Grid Margin="0,0,2,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <!-- <Menu>
            <MenuItem Header="{x:Static p:Resources.Menu_SandBox}">
                <MenuItem Header="{x:Static p:Resources.Menu_SandBox_DataGrid}" Command="{Binding SandBox}" CommandParameter="TestDataGridReadOnly" />
                <MenuItem Header="{x:Static p:Resources.Menu_SandBox_MasterDetail}" Command="{Binding SandBox}" CommandParameter="TestMasterDetail" />
            </MenuItem>
        </Menu>-->
        <TabControl x:Name="tabControl" Margin="10" Grid.Row="1">
            <TabItem x:Name="matchsTab" Header="Matchs">
                <ContentControl>
                    <local:MatchsView/>
                </ContentControl>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
