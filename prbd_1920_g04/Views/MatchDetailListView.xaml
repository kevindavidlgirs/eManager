<f:UserControlBase x:Class="prbd_1920_g04.Views.MatchDetailListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:prbd_1920_g04.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding ListMatchsView}" HorizontalAlignment="Left" AutoGenerateColumns="False" Height="26" VerticalAlignment="Top" Width="800" Margin="0,9,0,0">
        <DataGrid.Columns>
               <DataGridTextColumn Header="Date" Binding="{Binding Path=DateMatch, StringFormat=\{0:dd/MM/yyyy\}}" SortDirection="Descending"/>
                <DataGridTextColumn Header="Home" Binding="{Binding Path=Home}" Width="40" />
                <DataGridTextColumn Header="Home results" Binding="{Binding Path=GoalsHome}" />
                <DataGridTextColumn Header="Adversary results" Binding="{Binding Path=GoalsAdversary}" />
                <DataGridTextColumn Header="Adversary" Binding="{Binding Path=Adversary}" Width="40" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</f:UserControlBase>
